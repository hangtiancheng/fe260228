{
  "dependencies": {
    "dayjs": "^1.11.20",
    "md5": "^2.3.0"
  },
  "devDependencies": {
    "@types/md5": "^2.3.6",
    "concurrently": "^9.2.1",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.7.4"
  },
  "scripts": {
    "client:dev": "pnpm --filter @fe26/client dev",
    "server:dev": "pnpm --filter @fe26/server dev",
    "dev": "concurrently \"pnpm client:dev\" \"pnpm server:dev\"",
    "client:format": "pnpm --filter @fe26/client format",
    "server:format": "pnpm --filter @fe26/server format",
    "format": "concurrently \"pnpm client:format\" \"pnpm server:format\"",
    "git:commit": "git add -A && git commit -m \"feat: Introduce new features\"",
    "git:push": "pnpm git:commit && git push origin main"
  }
}
